"""Type aliases for entity IDs across the domain."""

from uuid import UUID

CompetitionId = UUID
SeasonId = UUID
TeamId = UUID
PlayerId = UUID
VenueId = UUID
RefereeId = UUID
MatchId = UUID
FeatureSnapshotId = UUID
ModelVersionId = UUID
PredictionId = UUID
DataSourceId = UUID
IngestionRunId = UUID
